name: My First Workflow

on: push

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print a message
        run: echo "The robot is awake"

      - name: Look around the machine
        run: pwd && ls -la